# Google Apps Script webhook URL — required for email signups to reach
# your Google Sheet. See SETUP_GOOGLE_SHEET.md for 2-minute setup.
GOOGLE_SHEETS_WEBHOOK_URL=

# If left blank in .env.local, the app still works for local testing —
# signups will log to the dev-server console instead of writing to Sheets.
